- clean:
    ~/:
        #force: true

- link:
    ~/.profile: profile
    ~/.bash_profile: bash_profile
    ~/.gitconfig: gitconfig
    ~/.git-commit-template: git-commit-template
    ~/.zshrc: zshrc
    ~/.nanorc: nanorc
